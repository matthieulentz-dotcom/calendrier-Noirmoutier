{
  "name": "calendrier-noirmoutier",
  "version": "1.0.0",
  "description": "Calendrier partagé maison de Noirmoutier",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "uuid": "^9.0.0",
    "concurrently": "^8.2.2"
  }
}
